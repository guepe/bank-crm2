<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Bank CRM{% endblock %}</title>
        <style>
            :root {
                --page-bg: linear-gradient(135deg, #f2efe7 0%, #d9e4dd 100%);
                --panel-bg: rgba(255, 255, 255, 0.92);
                --panel-border: rgba(33, 58, 45, 0.12);
                --text-main: #1e2d24;
                --text-muted: #56645d;
                --accent: #1f6f5f;
                --accent-strong: #124c40;
                --danger-bg: #f8dfdf;
                --danger-text: #8a2525;
                --input-border: #c8d4cd;
                --shadow: 0 20px 60px rgba(21, 42, 33, 0.14);
            }

            * {
                box-sizing: border-box;
            }

            body {
                margin: 0;
                min-height: 100vh;
                font-family: Georgia, "Times New Roman", serif;
                color: var(--text-main);
                background: var(--page-bg);
            }

            .shell {
                min-height: 100vh;
                padding: 24px;
            }

            .panel {
                width: min(100%, 1180px);
                margin: 0 auto;
                background: var(--panel-bg);
                border: 1px solid var(--panel-border);
                border-radius: 24px;
                box-shadow: var(--shadow);
                padding: 32px;
                backdrop-filter: blur(8px);
            }

            h1 {
                margin: 0 0 12px;
                font-size: clamp(2rem, 5vw, 2.8rem);
                line-height: 0.95;
                letter-spacing: -0.04em;
            }

            p,
            label,
            li {
                font-size: 1rem;
                line-height: 1.5;
            }

            .muted {
                color: var(--text-muted);
            }

            .alert {
                border-radius: 14px;
                padding: 12px 14px;
                margin-bottom: 18px;
            }

            .alert-danger {
                background: var(--danger-bg);
                color: var(--danger-text);
            }

            .stack {
                display: grid;
                gap: 14px;
            }

            .field {
                display: grid;
                gap: 6px;
            }

            input {
                width: 100%;
                border: 1px solid var(--input-border);
                border-radius: 12px;
                padding: 12px 14px;
                font: inherit;
                background: rgba(255, 255, 255, 0.9);
            }

            button,
            .button-link {
                display: inline-flex;
                align-items: center;
                justify-content: center;
                border: none;
                border-radius: 999px;
                padding: 12px 18px;
                font: inherit;
                font-weight: 700;
                color: #fff;
                background: var(--accent);
                text-decoration: none;
                cursor: pointer;
                transition: transform 160ms ease, background 160ms ease;
            }

            button:hover,
            .button-link:hover {
                background: var(--accent-strong);
                transform: translateY(-1px);
            }

            .topline {
                margin-bottom: 24px;
            }

            .nav {
                display: flex;
                flex-wrap: wrap;
                gap: 10px;
                align-items: center;
                justify-content: space-between;
                margin-bottom: 24px;
                padding-bottom: 20px;
                border-bottom: 1px solid var(--panel-border);
            }

            .nav-links,
            .actions {
                display: flex;
                flex-wrap: wrap;
                gap: 10px;
                align-items: center;
            }

            .grid {
                display: grid;
                gap: 18px;
            }

            .two-cols {
                grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            }

            .stats {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
                gap: 16px;
                margin-bottom: 24px;
            }

            .stat-card,
            .section-card {
                border: 1px solid var(--panel-border);
                border-radius: 18px;
                background: rgba(255, 255, 255, 0.7);
                padding: 18px;
            }

            .stat-card strong {
                display: block;
                font-size: 2rem;
                margin-top: 8px;
            }

            .stat-label {
                color: var(--text-muted);
            }

            .section-head {
                display: flex;
                flex-wrap: wrap;
                gap: 14px;
                align-items: center;
                justify-content: space-between;
                margin-bottom: 18px;
            }

            h2 {
                margin: 0 0 12px;
                font-size: 1.3rem;
            }

            .plain-list {
                margin: 0;
                padding-left: 18px;
            }

            .compact {
                gap: 10px;
            }

            .filter-grid {
                display: grid;
                gap: 12px;
                grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            }

            .active-filters {
                display: flex;
                flex-wrap: wrap;
                gap: 10px;
                margin: 16px 0 6px;
            }

            .filter-pill {
                display: inline-flex;
                align-items: center;
                border-radius: 999px;
                padding: 8px 12px;
                background: rgba(31, 111, 95, 0.1);
                color: var(--accent-strong);
                font-size: 0.95rem;
            }

            .status-pill {
                display: inline-flex;
                align-items: center;
                border-radius: 999px;
                padding: 6px 10px;
                font-size: 0.9rem;
                font-weight: 700;
                background: rgba(86, 100, 93, 0.12);
                color: var(--text-main);
            }

            .status-new {
                background: rgba(31, 111, 95, 0.12);
                color: var(--accent-strong);
            }

            .status-qualifying {
                background: rgba(187, 134, 0, 0.16);
                color: #7c5b00;
            }

            .status-proposal {
                background: rgba(30, 87, 153, 0.14);
                color: #1f4f86;
            }

            .status-won {
                background: rgba(24, 132, 73, 0.16);
                color: #16653c;
            }

            .status-lost {
                background: rgba(157, 46, 46, 0.14);
                color: #8a2525;
            }

            .secondary {
                background: #3f5f54;
            }

            .danger {
                background: #9d2e2e;
            }

            .data-table {
                width: 100%;
                border-collapse: collapse;
            }

            .data-table th,
            .data-table td {
                padding: 12px 10px;
                border-bottom: 1px solid var(--panel-border);
                text-align: left;
                vertical-align: top;
            }

            .info-list {
                display: grid;
                gap: 10px;
                margin: 0;
            }

            .info-list div {
                display: grid;
                gap: 3px;
            }

            .info-list dt {
                font-size: 0.9rem;
                color: var(--text-muted);
            }

            .info-list dd {
                margin: 0;
            }

            .timeline-list {
                list-style: none;
                margin: 0;
                padding: 0;
                display: grid;
                gap: 12px;
            }

            .timeline-item {
                display: grid;
                grid-template-columns: minmax(130px, 180px) 1fr;
                gap: 14px;
                padding-top: 12px;
                border-top: 1px solid var(--panel-border);
            }

            .timeline-item:first-child {
                padding-top: 0;
                border-top: none;
            }

            .timeline-date {
                font-size: 0.95rem;
                color: var(--text-muted);
            }

            .timeline-content {
                display: grid;
                gap: 4px;
            }

            .dashboard-hero {
                display: flex;
                flex-wrap: wrap;
                gap: 18px;
                align-items: flex-start;
                justify-content: space-between;
            }

            .dashboard-summary {
                display: grid;
                grid-template-columns: minmax(140px, 180px) 1fr minmax(220px, 280px);
                gap: 18px;
                align-items: stretch;
                margin-bottom: 22px;
                padding: 18px;
                border: 1px solid var(--panel-border);
                border-radius: 18px;
                background: rgba(255, 255, 255, 0.72);
            }

            .score-block {
                display: grid;
                place-items: center;
                align-content: center;
                min-height: 128px;
                border-radius: 16px;
                background: #e8f2ec;
                text-align: center;
            }

            .score-value {
                display: block;
                font-size: 2.4rem;
                font-weight: 700;
                line-height: 1;
                color: var(--accent-strong);
            }

            .summary-copy {
                display: grid;
                align-content: center;
                gap: 10px;
            }

            .missing-fields {
                display: flex;
                flex-wrap: wrap;
                gap: 8px;
            }

            .source-summary {
                display: grid;
                grid-template-columns: repeat(2, minmax(0, 1fr));
                gap: 10px;
                margin: 0;
            }

            .source-summary div {
                padding: 12px;
                border: 1px solid var(--panel-border);
                border-radius: 12px;
                background: rgba(255, 255, 255, 0.74);
            }

            .source-summary dt {
                color: var(--text-muted);
                font-size: 0.9rem;
            }

            .source-summary dd {
                margin: 4px 0 0;
                font-size: 1.35rem;
                font-weight: 700;
            }

            .dashboard-tabs {
                position: sticky;
                top: 0;
                z-index: 2;
                display: flex;
                flex-wrap: wrap;
                gap: 8px;
                margin: 22px 0;
                padding: 10px;
                border: 1px solid var(--panel-border);
                border-radius: 16px;
                background: rgba(255, 255, 255, 0.9);
            }

            .dashboard-tabs a {
                display: inline-flex;
                align-items: center;
                min-height: 40px;
                padding: 8px 12px;
                border-radius: 999px;
                color: var(--accent-strong);
                text-decoration: none;
                font-weight: 700;
            }

            .dashboard-tabs a:hover {
                background: rgba(31, 111, 95, 0.1);
            }

            .dashboard-sections {
                display: grid;
                gap: 22px;
            }

            .dashboard-tab {
                scroll-margin-top: 90px;
                border: 1px solid var(--panel-border);
                border-radius: 18px;
                background: rgba(255, 255, 255, 0.7);
                padding: 18px;
            }

            .dashboard-field-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
                gap: 14px;
            }

            .dashboard-field {
                display: grid;
                gap: 8px;
                min-width: 0;
                padding: 14px;
                border: 1px solid rgba(33, 58, 45, 0.1);
                border-radius: 14px;
                background: rgba(255, 255, 255, 0.78);
            }

            .dashboard-field label {
                display: flex;
                gap: 8px;
                align-items: center;
                justify-content: space-between;
                font-weight: 700;
            }

            .source-badge {
                display: inline-flex;
                align-items: center;
                min-height: 26px;
                padding: 4px 8px;
                border-radius: 999px;
                background: rgba(30, 87, 153, 0.12);
                color: #1f4f86;
                font-size: 0.82rem;
                font-weight: 700;
                white-space: nowrap;
            }

            .field-footer {
                display: flex;
                flex-wrap: wrap;
                gap: 10px;
                align-items: center;
                justify-content: space-between;
            }

            .field-footer button {
                min-height: 38px;
                padding: 9px 14px;
            }

            .timeline-dashboard {
                display: grid;
                gap: 14px;
                margin-top: 18px;
                padding-top: 18px;
                border-top: 1px solid var(--panel-border);
            }

            .timeline-editor-list {
                display: grid;
                gap: 12px;
            }

            .timeline-editor-item {
                display: grid;
                gap: 10px;
                padding: 14px;
                border: 1px solid rgba(33, 58, 45, 0.1);
                border-radius: 14px;
                background: rgba(255, 255, 255, 0.78);
            }

            .timeline-editor-main {
                display: grid;
                grid-template-columns: minmax(180px, 1.4fr) minmax(140px, 0.8fr) minmax(120px, 0.8fr) minmax(150px, 0.8fr);
                gap: 10px;
            }

            .timeline-editor-new {
                background: rgba(232, 242, 236, 0.72);
            }

            .form-shell form {
                display: grid;
                gap: 14px;
            }

            .form-shell label {
                display: block;
                margin-bottom: 6px;
            }

            textarea,
            select {
                width: 100%;
                border: 1px solid var(--input-border);
                border-radius: 12px;
                padding: 12px 14px;
                font: inherit;
                background: rgba(255, 255, 255, 0.9);
            }

            ul {
                line-height: 1.6;
            }

            a {
                color: var(--accent-strong);
            }

            @media (max-width: 720px) {
                .panel {
                    padding: 20px;
                }

                .dashboard-summary {
                    grid-template-columns: 1fr;
                }

                .dashboard-tabs {
                    position: static;
                }

                .timeline-editor-main {
                    grid-template-columns: 1fr;
                }

                .timeline-item {
                    grid-template-columns: 1fr;
                    gap: 6px;
                }
            }
        </style>
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    </head>
    <body>
        <main class="shell">
            <section class="panel">
                {% if app.user %}
                    <nav class="nav">
                        <div class="nav-links">
                            {% if is_granted('ROLE_USER') %}
                                <a href="{{ path('app_onboarding_index') }}">Accueil</a>
                                <a href="{{ path('app_dashboard') }}">Tableau de bord</a>
                                <a href="{{ path('app_lead_index') }}">Leads</a>
                                <a href="{{ path('app_contact_index') }}">Contacts</a>
                                <a href="{{ path('app_account_index') }}">Comptes</a>
                                <a href="{{ path('app_product_index') }}">Produits</a>
                                <a href="{{ path('app_document_index') }}">Documents</a>
                                {% if is_granted('ROLE_ADMIN') %}
                                    <a href="{{ path('app_user_index') }}">Utilisateurs</a>
                                {% endif %}
                                <a href="{{ path('app_profile') }}">Profil</a>
                            {% elseif is_granted('ROLE_CLIENT') %}
                                <a href="{{ path('app_portal_dashboard') }}">Mon espace</a>
                                <a href="{{ path('app_portal_contact_edit') }}">Mon dossier</a>
                            {% endif %}
                        </div>
                        <div class="actions">
                            <span class="muted">{{ app.user.displayName ?? app.user.userIdentifier }}</span>
                            <a class="button-link secondary" href="{{ path('app_logout') }}">Deconnexion</a>
                        </div>
                    </nav>
                {% endif %}
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert {{ label == 'success' ? '' : 'alert-danger' }}">{{ message }}</div>
                    {% endfor %}
                {% endfor %}
                {% block body %}{% endblock %}
            </section>
        </main>
    </body>
</html>
